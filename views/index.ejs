<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <% if(error){ %>
      <p>Error getting dynamic content.</p>
    <% } else{ %>
      <p>Loophaven Loft has the following records:</p>
      <ul>
        <li>Books: <%= data.item_count %></li>
        <li>Categories: <%= data.category_count %></li>
      </ul>
    <% } %>
  </body>
</html>
