<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1 class = 'title'><%= item.pattern %></h1>
    <div class = 'container-fluid'>
    <%- include('layout') -%>
    <div class = 'main-body'>
      <% if (item_patterns) { %>
        <h2><%= item.pattern %></h2>
        <p><%= item.description %></p>
        <p>Price: <%= item.price %></p>
        <p>Category: <a href = '<%= item.category.url %>'><%= item.category.name %></a></p>
      <% } else { %>
        <p>There are no patterns.</p>
      <% } %>
      <a href = '<%= item.url%>/delete'>
        <button type = 'submit'>Delete</button>
      </a>
    </div>
    </div>
  </body>
</html>